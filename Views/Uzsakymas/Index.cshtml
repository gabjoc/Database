@model IEnumerable<Org.Ktu.Isk.P175B602.Autonuoma.Models.Uzsakymas.UzsakymasL>

@* this will also be used as page title in main layout *@
@{ ViewData["title"] = "Užsakymai"; }

@* content header *@
<div class="main-content-header">
	<span class="title">@ViewData["title"]</span>
	<span class="actions">
		<a href="@Url.Action("Create")" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Sukurti naują</a>
	</span>
</div>

@* content itself *@
<div class="main-content-lst">
	<table class="entity-list">
		<thead>
			<tr>
				<th>
					@Html.DisplayNameFor(m => m.Uzsakymas)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.UzsakymoData)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Suma)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Busena)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Pristatymas)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Klientas)
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model) {
			<tr>
				<td>
					@Html.DisplayFor(m => item.Uzsakymas)
				</td>
				<td>
					@Html.DisplayFor(m => item.UzsakymoData)
				</td>
				<td>
					@Html.DisplayFor(m => item.Suma)
				</td>
				<td>
					@Html.DisplayFor(m => item.Busena)
				</td>
				<td>
					@Html.DisplayFor(m => item.Pristatymas)
				</td>
				<td>
					@Html.DisplayFor(m => item.Klientas)
				</td>
				<td>
					<a href="@Url.Action("Edit", new { id = item.Uzsakymas })" class="btn btn-primary"><i class="fa-solid fa-pen-to-square"></i> Redaguoti</a>
					<a href="@Url.Action("Delete", new { id = item.Uzsakymas })" class="btn btn-danger"><i class="fa-solid fa-trash"></i> Šalinti</a>
				</td>
			</tr>
			}
		</tbody>
	</table>
</div>
